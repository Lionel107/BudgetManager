// Modèle de configuration PWA.
// Copier en `config.js` (même dossier) et renseigner les valeurs du projet Supabase
// (Project Settings -> API). La anon key est publique par design (protégée par la RLS).
window.SUPABASE_CONFIG = {
  url: "https://VOTRE-PROJET.supabase.co",
  anonKey: "VOTRE_ANON_KEY",
};
